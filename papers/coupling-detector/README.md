# Coupling-detector paper workspace

This is the active publication track.

- [`RESULT_NOTE.md`](RESULT_NOTE.md) gives the concise scientific result.
- [`PROSPECTUS.md`](PROSPECTUS.md) is the current theorem and assumption audit.
- [`HUMAN_PROOF.md`](HUMAN_PROOF.md) is the end-to-end derivation and exact
  boundary between the proved invariant-EMD necessity/unique-closure
  confluence result, its final packaging step, and the later local converse.

The central result is an exact third-order non-identifiability theorem plus a
generic fourth-order recovery formula for the squared EMD coupling. The
conditional theorem now produces a finite accepted actual-metric choice with
output `a²` from explicit physical data on a selected open upstream patch and
returns `3` under Kaluza normalization. The detector-choice-free physical EMD
package, scalar/coupling sign alignment, intersection of scalar and upstream
germs, and end-to-end finite-selector composition are now proved. Conventional
`C²` metric/residual/`qSq` regularity now derives the selected coframe and
magnitude regularity. The choice-free physical complexion one-form now
derives the effective channel and identifies the physical Maxwell-complexion
activity condition exactly with the detector gate; the active set is open
under continuity, with no density claim. On the sharp locus where
the two scalar-closure obstructions do not both vanish, every pointwise
accepted survivor returns physical `a²` under the displayed conventional
local regularity and probe hypotheses. The final physical-active wrapper now
composes these ingredients into metric-only nonemptiness with output `a²`
(`3` for Kaluza). Complete-detector benchmark routing, theorem exposition and
novelty validation, and the full converse remain. The older
`papers/paper-01/` directory is the fallback scalar-reconstruction paper, not
the active theorem narrative.
